meta:
  environment: jvm
  toolchain:
    name: gradle
    version: "17"
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: ntitestru.ntitest-all.jar
  persistenceMount: /data
  containerPort: 80
